# Panoptique Ferroviaire - Environment Variables Example
# Copy this file to .env and customize as needed
#
# === MODE AUTONOME (SANS DOCKER) ===
# Pour exécuter en mode autonome sans Kafka/PostgreSQL:
#   python main_standalone.py
#
# === MODE COMPLET (AVEC DOCKER) ===
# Pour exécuter avec tous les services:
#   docker-compose up -d
#   python main.py
#

# ==============================================================================
# KAFKA CONFIGURATION (Non requis en mode autonome)
# ==============================================================================
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
KAFKA_TOPIC_RAW_TELEMETRY=raw_telemetry
KAFKA_GROUP_ID=neural_engine

# ==============================================================================
# POSTGRESQL CONFIGURATION (Non requis en mode autonome)
# ==============================================================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=panoptique
POSTGRES_USER=panoptique
POSTGRES_PASSWORD=panoptique_secure_2024

# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================
LOG_LEVEL=INFO
SIMULATION_RATE=1.0
HARVEST_INTERVAL=30.0

# ==============================================================================
# PERFORMANCE
# ==============================================================================
UVLOOP_ENABLED=true
MAX_CONCURRENT_HARVESTS=20

# ==============================================================================
# STATIC DATA PATHS (Pour Rail-Lock et Holographic Positioning)
# ==============================================================================
# Ces fichiers sont générés par: python -m src.tools.prepare_data
TOPOLOGY_PATH=data/topology.json
STOPS_PATH=data/stops.txt

# ==============================================================================
# MODE AUTONOME (Standalone)
# ==============================================================================
# Nombre maximal de pages à scanner lors de la découverte (limite la mémoire)
STANDALONE_MAX_DISCOVERY_PAGES=5
# Intervalle d'affichage du statut en secondes
STANDALONE_STATUS_INTERVAL=30
